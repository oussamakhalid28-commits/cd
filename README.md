{
  "name": "cd",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "echo \"Using www as webDir - no build step\"",
    "prepare": "npx cap sync",
    "android": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/core": "^5.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.0.0"
  }
}
